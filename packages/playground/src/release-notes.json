{
  "devbar": {
    "1.11.0": "Portless compatibility for proxied local development. devbar now reads Sweetlink runtime connection hints instead of assuming the visible browser port is the internal app port, supports manual `sweetlink` overrides (`appPort`, `wsPort`, `wsUrl`, `wsPath`), tries same-origin WebSocket endpoints before direct localhost ports, and keeps explicit-port localhost workflows working. Bumps @ytspar/sweetlink to 1.23.0.",
    "1.10.8": "Responsive toolbar and docs-site hardening. Custom/plugin controls now keep long Web Vitals, action buttons, and plugin rows inside the expanded toolbar at desktop and tablet widths; phone expanded toolbar spacing is tighter while preserving 44px touch targets. The playground homepage adds a plugin-surface section with an interactive Zaraz consent-debug, git branch, and app-version demo plus a concrete consentDebugPlugin example. Mobile package cards and fixed simulated-mode controls are guarded by new screenshot/evidence e2e coverage. Bumps @ytspar/sweetlink to 1.22.1 for automatic Playwright screenshot hiding helpers.",
    "1.10.7": "Bump @ytspar/sweetlink to 1.22.0. Modal accessibility: dialog elements now carry `aria-label` reflecting their title and auto-focus on open (Outline, Schema, A11y, Console, Design Review, Consent Debug). Code cleanup: deleted dead `ui/cards.ts` (163 lines), collapsed `modules/rendering.ts` barrel, pruned unused html2canvas helpers, extracted `addSavedTooltip` for the 4 button creators. Test fix: axe e2e scope corrected (`include` selector moved from `RunOptions` to context arg — was scanning entire page). Responsive plugin hardening: custom toolbar controls now wrap inside expanded and compact devbar layouts, cap long labels, center mobile group labels, preserve 44px touch targets, and ship e2e screenshots for arbitrary plugin controls at 320, 390, 640, 836, and 993px.",
    "1.10.6": "Bump @ytspar/sweetlink to 1.21.0 (Android tap-indicator overlays).",
    "1.10.5": "Bump @ytspar/sweetlink to 1.20.0 (multi-capture JSON-stdin batching + app/date/run directory hierarchy).",
    "1.10.4": "Bump @ytspar/sweetlink to 1.19.0 (terminal capture mode + iOS/Android simulator screen recording — `proof`-style evidence-of-work for non-browser tests).",
    "1.10.3": "Bump @ytspar/sweetlink to 1.18.0 (recording pause/resume, sessions list+index, record-exec DSL, --label/--theme/--storage-state/--trace flags, click-css occlusion detection, network body capture, visual-diff side-by-side viewer, brighter cursor highlight, real action durations, ISO session names, failure screenshots, compact CLI output — 20 UX wins).",
    "1.10.2": "Bump @ytspar/sweetlink to 1.17.0 (recording bug fixes, screenshot full-page dim reporting, snapshot annotation full-page capture, click-css daemon action, action-screenshot click marker, ISO timestamps, and 14 more UX polishes — see sweetlink 1.17.0 release notes for details).",
    "1.10.1": "Republish of 1.10.0 via pnpm so the @ytspar/sweetlink dependency resolves to a real version instead of `workspace:^`. 1.10.0 is deprecated — use 1.10.1.",
    "1.10.0": "Consent-debug plugin (@ytspar/devbar/plugins/consent-debug): toolbar control gated on window.zaraz that opens a modal for mocking geo regions, triggering banner/notice events, Accept/Reject/Clear/Reset actions, and a live status readout — generic cookie and event names",
    "1.9.2": "Demo button + HiFi screenshot (Alt+Click) in toolbar, recording auto-opens viewer, demo document state fields",
    "1.9.1": "Bump sweetlink dependency to v1.14.0 (persistent daemon, ref system, ring buffers, session recording)",
    "1.9.0": "Official plugins (git-branch, app-version), server info on state (gitBranch, appName), PALETTE.white, root tsconfig with project references, 232 new tests",
    "1.8.4": "Fix custom controls not showing: store controls on window to survive bundler module duplication, render controls in compact mode",
    "1.8.0": "Shadow theme tokens, CSS_COLORS theming fixes, function extraction, noUncheckedIndexedAccess",
    "1.7.5": "Correct ruler icon SVG, PALETTE color system centralization",
    "1.7.1": "Ruler tool for measuring element dimensions on-page",
    "1.6.4": "Console log sync, CLI shutdown coordination",
    "1.6.0": "Inline DepartureMono font, capture browser-level errors",
    "1.5.3": "CSP bypass via Playwright fallback for exec/click/query",
    "1.5.0": "Control groups, info variant, and non-interactive badges",
    "1.4.0": "DRY modal copy/download, pre-release audit, expanded tests",
    "1.3.0": "Accessibility audit, document outline, schema extraction",
    "1.2.0": "Screenshot quality slider, settings crash fix",
    "1.1.0": "Screenshot improvements, browser download mode",
    "1.0.0": "Initial release — breakpoints, vitals, console, screenshots"
  },
  "sweetlink": {
    "1.23.0": "Portless and named-localhost routing support. Vite integration now exposes a same-origin `/__sweetlink` WebSocket endpoint and injects runtime app/WebSocket port hints so HTTPS Portless URLs with hidden internal ports connect automatically. Next.js integration injects public Sweetlink port hints. Browser bridge/devbar shared helpers now support explicit wsUrl/wsPath/appPort/wsPort overrides. Server origin handling accepts `.localhost`, `.test`, and LAN `.local` development origins, avoids false internal-port warnings for named proxy origins, and keeps direct localhost port mismatch warnings.",
    "1.22.1": "Playwright screenshot auto-hide helpers. `installAutoHideDevbarScreenshots(page)` patches `page.screenshot()` so every Playwright user screenshot temporarily hides `[data-devbar]`, overlays, and tooltips, then restores them after capture. `installAutoHideDevbarScreenshotsForContext(context)` applies the same behavior to existing and future pages in a BrowserContext. Existing explicit paths remain: CLI `--hide-devbar`, `screenshotViaPlaywright({ hideDevbar: true })`, `withHiddenDevbarForScreenshot(page, ...)`, and browser `prepareForCapture({ hideDevbar: true })`. Docs now include a Playwright fixture example.",
    "1.22.0": "**Hardening release** — 12 security/bug fixes + 11 type-design improvements + 10 cleanups from a multi-agent pre-release audit. **Security (P0):** XSS in term player .html — recorded shell output containing `</script>` previously broke out of the inline data block; now escaped via `escapeJsonForScript` (also handles `<!--`, U+2028/U+2029). Path traversal — `--app ../../tmp/x` and `--run ../../etc/cron.d` no longer escape `.sweetlink/` (segments are slugified). Batch JSON `--output` now refuses paths outside the project root by default (`SWEETLINK_ALLOW_OUTSIDE_OUTPUT=1` opts out). **Bug class (P1):** spawned commands now have `error` handlers + try/finally cleanup so missing-shell/bad-cwd no longer orphan recorders or `getevent` listeners. `term`/`sim` default to a minimal env (PATH/HOME/USER/LANG/SHELL/etc.) so secrets in env (NPM_TOKEN, GH_TOKEN, ANTHROPIC_API_KEY, …) can't leak into recorded artifacts; `--inherit-env` opts back in. `visualDiff` distinguishes baseline-missing (ENOENT on baseline) from real IO errors and surfaces the message instead of returning `100% mismatch`. `term player` parses asciicast safely (try/catch with diagnostic, version validation, refuses empty events, clamps duration). `getevent -l` parsing rejects non-hex/out-of-range values and clamps to screen bounds (was producing NaN that silently broke ffmpeg). Android pull now polls for stat-size stability before `adb pull` (replacing a fixed 500ms sleep that caused truncated mp4s on slow devices). Tap-capture proc waits for `close` and flushes its trailing buffer (taps in the last few hundred ms before SIGTERM are no longer dropped). **Type design:** dropped 8 stale `demo-*` entries from `DaemonAction` that had no handlers; tightened `ConsoleLog.level` from `string`-erased to `ConsoleLogLevel` literal union; new shared `term/cast.ts` types (recorder + player); new `RecordedAction` union for `logAction`; dropped `''` empty-string sentinel from `RunSlotOptions.kind`. **Cleanup:** deleted unused `term/ansi.ts` (202 lines); consolidated 3 copies of `escapeHtml` and 2 of `detectGit` into `daemon/utils.ts`; new `findLatestSessionDir`, `openInBrowser`, `resolveCapturePath` helpers (batch path no longer bypasses `runSlot()`); replaced nested ternaries with `thresholdColor` helper for Web Vitals. **Deps:** pnpm overrides for picomatch/postcss/brace-expansion (18 → 6 audit advisories; remaining 6 are dev-only — vite 7 dev server + basic-ftp transitive via puppeteer-core proxy chain). 14 new unit tests (cast.test.ts, runs.test.ts).",
    "1.21.0": "**Android tap-indicator overlays** for `sim android` recordings. While `adb shell screenrecord` captures the screen, a parallel `adb shell getevent -l` listener parses BTN_TOUCH DOWN/UP + ABS_MT_POSITION_X/Y events into (x, y, t) tuples. Coordinates are scaled from raw input-device units to screen pixels using `wm size`. After the test command exits, `ffmpeg drawbox` composites a red hollow ring + centred solid dot at each tap position for 0.6s, baked into the .mp4. A `.taps.json` sidecar is always written; if ffmpeg isn't on PATH the raw recording is preserved and a clear hint is logged. `--no-overlays` opts out entirely. 14 unit tests (parser + filter builder) + 2 e2e tests synthesise a black mp4 and verify red rings render at the right times — visually confirmed at the (120, 200) coordinate. iOS tap overlays remain a follow-up requiring `idb_companion`.",
    "1.20.0": "Two more `proof`-parity items: **multi-capture JSON-stdin batching** and **app/date/run directory hierarchy**. `cat captures.json | sweetlink --json` reads `{action:'capture',captures:[...]}` and runs each entry (term/sim-ios/sim-android/screenshot), aggregating results into a single envelope on stdout — failing entries set `ok:false` for the run, exit code 1. `--app <name>` groups artifacts under `.sweetlink/<app>/<YYYYMMDD>/<run>/<kind>/...`; `--run <id>` (or `SWEETLINK_RUN` env) pins the run id for related captures; without `--app` the historical flat layout is preserved. `--app` no longer triggers `discoverServer` for term/sim (they don't need a WS bridge), so a typo'd app name doesn't error out artifact-producing commands. 9 new e2e tests in `e2e/batch-mode.spec.ts` (5) and `e2e/run-hierarchy.spec.ts` (4). Curl-pipe-sh installer was considered and skipped — sweetlink is npm-distributed, the installer would be a thin wrapper around `npm i -g`.",
    "1.19.0": "Two new capture modes inspired by automazeio/proof — turning sweetlink into evidence-of-work for non-browser tests. **`sweetlink term <command>`** runs any shell command (pytest, go test, make build) and writes asciicast v2 (`.cast`) plus a self-contained HTML player with play/pause, 0.1×–4× speed control, seek bar, and ANSI colour rendering. Auto-speed up for long recordings, exit-code propagation by default (CI fails when wrapped tests fail; `--ignore-exit` opts out). **`sweetlink sim ios|android <command>`** records the iOS Simulator (via `xcrun simctl io recordVideo`) or Android Emulator (via `adb shell screenrecord`) screen while the test command runs, producing an h264 .mp4 of what was on screen. iOS path tested end-to-end on iPhone 16e (95KB / 2.5s native-res capture verified). Player UX confirmed visually: ANSI colours render, speed controls work, seek bar follows playback. New e2e specs: `e2e/term-mode.spec.ts` (5 tests) and `e2e/sim-mode.spec.ts` (6 tests with platform-aware skips).",
    "1.18.0": "Round 2 of UX polish — 20 improvements ranging from output cleanup to new features. Quick wins: compact `screenshot ✓ path · WxH · KB · method` one-liner; ActionEntry.duration now populated from actual wall-clock; session IDs are ISO timestamps (`session-2026-04-26T19-47-14`) instead of millisecond epochs; cross-platform viewer auto-open via `open`/`xdg-open`/`cmd /c start`; daemon prints an idle warning at 80% of timeout; top-level `--help` lists one-line summaries with `--all` for full per-command details; --json now works for status/cleanup/wait. Recording: action screenshots stash to `.sweetlink/failures/` on click/fill errors with diagnostic name; `record start --label \"login flow\"` embeds in manifest + SUMMARY title; `record pause` / `record resume` gap the action timeline so idle windows don't pollute the recording; cursor click ripple now larger, longer, with persistent center dot. Sessions: `sweetlink sessions list` shows every recording with label/duration/error counts; `sessions open` opens an auto-generated `.sweetlink/index.html` browse page; `sessions diff <a> <b>` compares two recordings (action sequences, durations, error counts). Big features: `record exec \"click @e2; fill @e3 hello; press Enter\"` runs a one-liner DSL with auto start/stop; `record start --storage-state path.json` for testing logged-in flows; `record start --trace` writes Playwright `trace.zip` for `playwright show-trace`; `screenshot --theme dark` emulates `prefers-color-scheme`; `network-read --withBody` captures truncated request/response bodies; `visual-diff` writes a side-by-side `.diff.html` with toggle/overlay (CSS difference blend mode) — far more actionable than just a percentage; `click-css` occlusion check detects overlay covering and reports `covered by <#modal>` instead of a 30s timeout. 10 new e2e tests in `e2e/wave-features.spec.ts`.",
    "1.17.0": "TDD audit shipping 9 daemon/CLI bug fixes + 17 UX polishes. Fixes: recording manifest now counts in-session console errors and network failures (was hardcoded 0); screenshots and CSS-selector clicks during recording route to the recording page and get logged as actions; --full-page reports actual PNG dims (not viewport); click-ref/fill-ref fail fast on disabled/non-fillable elements (was 30s hang); console-read/network-read auto-init the browser so first calls aren't empty; daemon status/stop honour --url. New: click-css daemon action, --padding flag for selector screenshots, multi-match selector warning, --full-page hint when content overflows, --responsive defaults to fullPage, viewer.html references session.webm instead of inlining (~280× smaller HTML), recording action screenshots show a red click marker, ruler dim labels positioned outside bbox with bigger font, ruler captures full page when elements extend past viewport, snapshot --annotate captures full page + filters off-screen refs, SUMMARY action timeline is humanized and links to action screenshots, SUMMARY uses ISO 8601 timestamps with timezone, SUMMARY surfaces git-not-in-repo state, recording viewport now matches DEFAULT_VIEWPORT (1512×982). 52 new e2e tests gating these in `e2e/*-bugs.spec.ts`.",
    "1.16.1": "Add `default` condition to subpath exports so non-ESM consumers (Node --test, tsx in CJS) can resolve imports like @ytspar/sweetlink/browser/consoleCapture without hitting ERR_PACKAGE_PATH_NOT_EXPORTED",
    "1.16.0": "Bundle Claude Code skills (screenshot, console-check, responsive-screenshots, resize-for-claude), agent (sweetlink-visual-iterative), and context files (architecture, CDP guide, UI verification mandate) — shipped with npm package for automatic discovery",
    "1.15.0": "Demo document builder (Showboat-inspired), universal sharing (report --clipboard/--serve/--webhook), viewer share buttons (Copy Report, Download), multi-language server error detection, SUMMARY.md auto-generation, git metadata in recordings, overlay toggle, responsive viewer, HiFi screenshots from devbar (Alt+Click), comparison tables in docs",
    "1.14.0": "Sweetlink v2: persistent Playwright daemon (--hifi, --responsive), @ref system from accessibility tree (snapshot -i, click @e3, fill @e4), snapshot diffing (-D) and annotated screenshots (-a), always-on console/network ring buffers, session recording with interactive viewer, cursor highlights, visual diff, device emulation, PR evidence upload",
    "1.13.0": "Next.js plugin: withSweetlink() config wrapper for zero-config dev server integration, auto-detects port from --port argv",
    "1.12.1": "Fix navigateBrowser URL verification: poll until actual URL matches target to prevent premature capture on SPAs",
    "1.12.0": "App identity: server detects git branch/app name, CLI --app flag targets specific instance for parallel worktree dev",
    "1.11.1": "Biome formatting and lint, README updates",
    "1.11.0": "--url browser navigation, optional peerDeps for puppeteer-core/html2canvas-pro, docs overhaul",
    "1.10.0": "--json and --output-schema flags for structured CLI output",
    "1.9.0": "Wait-for selector, async exec, return statement auto-wrap",
    "1.8.1": "Console log buffer sync, 209 new tests",
    "1.8.0": "CLI shutdown synchronization with dev server lifecycle",
    "1.6.0": "Console log eviction sync, browser error capture",
    "1.5.0": "CSP eval-blocked detection + Playwright fallback",
    "1.4.0": "Dead code removal, deduplicated constants, 85 new tests",
    "1.3.0": "CDP and ruler test suites, ruler/playwright sub-exports",
    "1.2.0": "Outline and schema tools moved from devbar, modal UX",
    "1.1.0": "Auto-detect save location, browser download mode",
    "1.0.0": "Initial release — screenshots, logs, HMR capture, CLI"
  }
}
